# packaging/

签名资产目录。请把你的 **Developer ID 类型 provisioning profile** 放到本目录，并命名为
`esmvp.provisionprofile`（或构建时用 `PROFILE=/path/to/xxx.provisionprofile` 指定）。

获取方式见仓库根 README.md 的「开始前你需要准备」。
`*.provisionprofile` 已被 .gitignore 忽略，请勿提交到仓库。
